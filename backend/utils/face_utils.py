"""Face utils stub — not used in lightweight/free-tier build."""
from typing import Tuple

def detect_and_crop_faces(image, padding: float = 0.2) -> Tuple[list, int]:
    """Stub: always returns no faces detected."""
    return [], 0
