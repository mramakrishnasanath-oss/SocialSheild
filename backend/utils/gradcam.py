"""Grad-CAM stub — not used in lightweight/free-tier build."""
import logging
logger = logging.getLogger(__name__)

class GradCAMGenerator:
    def __init__(self, model=None, target_layer_name: str = ""):
        pass
    def generate(self, *args, **kwargs):
        return None
